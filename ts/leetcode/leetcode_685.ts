function findRedundantDirectedConnection(edges: number[][]): number[] {
    const edgesMap: { [key: number]: number } = {};

    for (let i = 0; i < edges.length; i++) {
        edgesMap
    }

    return [];
};